export async function screenshot():Promise<string>{const canvas=document.createElement("canvas");const width=Math.min(1200,Math.max(320,document.documentElement.clientWidth));const height=Math.min(900,Math.max(200,document.documentElement.clientHeight));canvas.width=width;canvas.height=height;const ctx=canvas.getContext("2d");if(!ctx)throw new Error("Screenshot unavailable");ctx.fillStyle="#fff";ctx.fillRect(0,0,width,height);ctx.fillStyle="#111";ctx.font="14px sans-serif";const text=(document.body?.innerText||"").slice(0,4000);text.split(/\n+/).reduce((y,line)=>{ctx.fillText(line.slice(0,140),8,y);return y+18;},20);return canvas.toDataURL("image/png");}