import { findElement } from "../dom/state"; export function select(target:string,value:string):void{const e=findElement(target) as HTMLSelectElement|null;if(!e||e.tagName!=="SELECT")throw new Error(`Select not found: ${target}`);const option=Array.from(e.options).find(o=>o.value===value||o.text===value);if(!option)throw new Error("Option unavailable");e.value=option.value;e.dispatchEvent(new Event("change",{bubbles:true}));}