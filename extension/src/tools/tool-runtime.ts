import { Action } from "../types"; import { click } from "./click"; import { fill } from "./fill"; import { select } from "./select"; import { scroll } from "./scroll"; import { pressKey } from "./keyboard"; import { navigate } from "./navigate"; import { wait } from "./wait"; import { resolveProfile } from "../profile/profile"; import { screenshot } from "./screenshot";
export async function execute(action:Action):Promise<void>{switch(action.action){case"click":return click(required(action.target));case"fill":return fill(required(action.target),action.value_source?await resolveProfile(action.value_source):required(action.value));case"select":return select(required(action.target),required(action.value));case"scroll":return scroll(action.direction||"down");case"press_key":return pressKey(required(action.key),action.target);case"navigate":return navigate(required(action.url));case"wait":return wait(action.duration_ms||0);case"screenshot":await screenshot();return;case"upload":await resolveProfile(required(action.value_source));throw new Error("Upload requires an explicit local file selection; no file contents are available to the agent runtime");default:throw new Error(`Unsupported action: ${action.action}`);}}
function required(value:string|undefined):string{if(!value)throw new Error("Action argument missing");return value;}