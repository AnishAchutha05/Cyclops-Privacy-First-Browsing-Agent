import { findElement } from "../dom/state";
export function fill(target:string,value:string):void{const e=findElement(target) as HTMLInputElement|HTMLTextAreaElement|null;if(!e)throw new Error(`Element not found: ${target}`);e.focus();const proto=e instanceof HTMLTextAreaElement?HTMLTextAreaElement.prototype:HTMLInputElement.prototype;const setter=Object.getOwnPropertyDescriptor(proto,"value")?.set;if(setter)setter.call(e,value);else e.value=value;e.dispatchEvent(new Event("input",{bubbles:true}));e.dispatchEvent(new Event("change",{bubbles:true}));}
