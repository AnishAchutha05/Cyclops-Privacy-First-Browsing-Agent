import { findElement } from "../dom/state"; export function fill(target:string,value:string):void{const e=findElement(target) as HTMLInputElement|null;if(!e)throw new Error(`Element not found: ${target}`);e.focus();const setter=Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,"value")?.set;if(setter)setter.call(e,value);else e.value=value;e.dispatchEvent(new Event("input",{bubbles:true}));e.dispatchEvent(new Event("change",{bubbles:true}));}