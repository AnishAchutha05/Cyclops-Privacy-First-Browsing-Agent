import { extractElements } from "../dom/extractor"; import { sanitizeText,sanitizeUrl } from "../privacy/sanitizer"; import { SanitizedPageContext } from "../types";
export function buildContext(doc:Document=document):SanitizedPageContext{return {page_title:sanitizeText(doc.title),page_url_base:sanitizeUrl(location.href),elements:extractElements(doc),sanitization_note:"PII, file paths, query parameters, and fragments are removed locally."};}
export function serializeContext(c:SanitizedPageContext):string{return JSON.stringify(c);}