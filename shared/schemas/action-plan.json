{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://cyclops/schemas/action-plan",
  "title": "ActionPlan",
  "description": "A structured list of browser actions returned by the Cyclops backend.",
  "type": "object",
  "required": ["actions"],
  "additionalProperties": false,
  "properties": {
    "actions": {
      "type": "array",
      "description": "Ordered list of actions for the extension to execute.",
      "minItems": 1,
      "items": { "$ref": "action.json" }
    }
  }
}
